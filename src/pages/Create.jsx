import { Typography } from "@mui/material";

export default function Create() {
  return (
    <div>
      <Typography
        variant="h6"
        color="textSecondary"
        component="h2"
        gutterBottom="true"
      >
        Create a New Note
      </Typography>
    </div>
  );
}
